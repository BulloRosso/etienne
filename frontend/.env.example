# Portal App host — when set, Vite proxies /app/* to this host.
# Per-project enabling still requires `appDirectory` in the project's
# .etienne/user-interface.json (configurable via Customization UI).
PORTAL_APP_HOST=http://localhost:5001
