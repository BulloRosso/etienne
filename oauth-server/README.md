# Minimal Implementation

This OAuth server should be replaced in a deployment with the cloud platform **identity and access management (IAM)** like Entra on Microsoft Azure.

Do not use in production - this services is only included for a self-contained development environment!